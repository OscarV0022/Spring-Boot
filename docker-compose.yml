version: '3.8'
services:
  app-spring:
    # Usamos la variable que le pasaremos desde GitHub Actions
    image: ${REGISTRY_IP}:5000/mi-app-spring:latest
    ports:
      - "80:8080"
    restart: always